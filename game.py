# 1.リストの要素の平均値を求める
def mean(numbers):
    ave = sum(numbers) / len(numbers)
    num = len(numbers)
    print(f"{num}つの要素の平均値は{ave}です")


mean([1, 2, 3, 4, 5])
# 2.内積を求める
# 3.回文かどうか判定する
# 4.フィボナッチ数列のn番目と求める
# 5.リスト内で最大値・最小値を求める
